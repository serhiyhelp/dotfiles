#!/bin/sh

case $BLOCK_BUTTON in
    1) xkb-switch -n ;;
    6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

case $(xkb-switch) in
    'us') echo -e "🐥 eng\x02" ;;
	'ua(winkeys)') echo -e "🐥 укр\x02" ;;
esac
